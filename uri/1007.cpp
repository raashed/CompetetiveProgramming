#include <bits/stdc++.h>

using namespace std;

int main()
{
    int A, B, C, D;
    while (cin >> A >> B >> C >> D)
    {
        cout << "DIFERENCA = " << (A * B - C * D) << endl;
    }

    return 0;
}
