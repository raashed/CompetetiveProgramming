#include<iostream>
#include<vector>
#include<string>
#include<algorithm>

using namespace std;

void fastIO() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

#ifndef ONLINE_JUDGE
    FILE *stream;
    freopen_s(&stream, "../in.txt", "r", stdin);
#endif
}

int main() {
    fastIO();

    return 0;
}
